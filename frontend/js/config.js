const CONFIG = {
    // Change this when you deploy your backend
    API_BASE_URL: 'http://localhost:3000',

    // Socket.IO server URL (same as backend for now)
    SOCKET_URL: 'http://localhost:3000'
};
